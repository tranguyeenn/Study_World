## Folder Overview
- `config/` — environment + Supabase connection
- `routes/` — Flask Blueprints (API endpoints)
- `controllers/` — logic behind routes
- `models/` — database structure and helpers
- `utils/` — helpers (auth, validation, error handling)
